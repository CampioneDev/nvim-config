return {
  -- on_attach = function(client)
  --   -- we disable hover because it conflicts with TS server's hover
  --   client.server_capabilities.hoverProvider = false
  --   client.server_capabilities.definitionProvider = false
  -- end,
}
