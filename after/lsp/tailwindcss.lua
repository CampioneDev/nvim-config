-- local lspconfig_tailwindcss_config = {}
-- local lspconfig_plugin = require('lazy.core.config').plugins['nvim-lspconfig']
-- if lspconfig_plugin then
--   lspconfig_tailwindcss_config = dofile(lspconfig_plugin.dir .. '/lsp/tailwindcss.lua')
-- end

-- print(vim.inspect(lspconfig_tailwindcss_config))

return {
  -- settings = {
  --   tailwindCSS = {},
  -- },
}
